import AssetManager = Games.AssetManager;
import ConfigManager = configs.ConfigManager;

class Game
{
    static asset: AssetManager = new AssetManager();
    static config: ConfigManager = new ConfigManager();

    static install()
    {
        
    }

}